const DepartmentHeading = ({ heading }) => {
	return (
		<div className="heading-container">
			<h3 className="heading">{heading}</h3>

			<span className="line" />
		</div>
	);
};

export default DepartmentHeading;
